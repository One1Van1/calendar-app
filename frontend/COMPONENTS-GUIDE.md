# Структура Frontend - Детализированная

## 📁 Архитектура по компонентам

Каждый визуальный элемент разделён на **логику** и **отображение**:

```
frontend/renderer/components/
├── CalendarDay/              # ДЕНЬ КАЛЕНДАРЯ
│   ├── day.logic.js          # Логика: события, состояния, клики
│   └── day.view.js           # Отображение: цвета, размеры, стили
│
├── CalendarGrid/             # СЕТКА КАЛЕНДАРЯ
│   ├── grid.logic.js         # Логика: дни недели, расчёт строк
│   └── grid.view.js          # Отображение: фон сетки, отступы, заголовки
│
├── CategoryItem/             # ЭЛЕМЕНТ КАТЕГОРИИ
│   ├── category.logic.js     # Логика: активность, клики
│   └── category.view.js      # Отображение: цвет индикатора, ховер
│
├── SidebarBackground/        # ФОН БОКОВОЙ ПАНЕЛИ
│   ├── sidebar-bg.logic.js   # Логика: сворачивание, состояние
│   └── sidebar-bg.view.js    # Отображение: цвет фона, границы
│
├── MainBackground/           # ФОН ОСНОВНОЙ ОБЛАСТИ
│   ├── main-bg.logic.js      # Логика: загрузка, состояния
│   └── main-bg.view.js       # Отображение: цвет фона, отступы
│
├── Header/                   # ШАПКА
│   ├── header.logic.js       # Логика: навигация, переключение темы
│   └── header.view.js        # Отображение: иконки, текст месяца
│
├── Sidebar/                  # БОКОВАЯ ПАНЕЛЬ
│   ├── sidebar.logic.js      # Логика: загрузка категорий
│   └── sidebar.view.js       # Отображение: рендер списка
│
├── Calendar/                 # КАЛЕНДАРЬ
│   ├── calendar.logic.js     # Логика: загрузка событий, даты
│   └── calendar.view.js      # Отображение: сборка компонентов
│
└── Modal/                    # МОДАЛЬНОЕ ОКНО
    ├── modal.logic.js        # Логика: создание событий, валидация
    └── modal.view.js         # Отображение: форма, поля
```

---

## 🎨 Где менять ОТОБРАЖЕНИЕ (внешний вид)

### 📅 **Изменить цвет/вид дня календаря**
Файл: `components/CalendarDay/day.view.js`

```javascript
styles: {
    backgroundColor: 'transparent',           // Фон дня
    backgroundColorHover: 'rgba(255, 255, 255, 0.2)',  // Фон при наведении
    todayBackgroundColor: '#4a6b8a',         // Фон текущего дня
    textColor: '#ffffff',                    // Цвет текста
    numberFontSize: '1rem',                  // Размер числа
    eventDotSize: '6px',                     // Размер точек событий
    borderRadius: '6px',                     // Скругление углов
}
```

### 🌐 **Изменить фон/вид сетки календаря**
Файл: `components/CalendarGrid/grid.view.js`

```javascript
styles: {
    backgroundColor: '#5b7a9b',              // Цвет фона сетки
    borderRadius: '8px',                     // Скругление углов
    padding: '1rem',                         // Отступы
    weekdayColor: '#ffffff',                 // Цвет дней недели
    gridGap: '0.5rem',                       // Расстояние между днями
}
```

### 📂 **Изменить фон боковой панели**
Файл: `components/SidebarBackground/sidebar-bg.view.js`

```javascript
styles: {
    backgroundColor: '#5b7a9b',              // Цвет фона
    width: '250px',                          // Ширина
    borderRight: '1px solid rgba(255, 255, 255, 0.1)',  // Граница
    padding: '1rem',                         // Отступы
}
```

### 🖼️ **Изменить фон основной области**
Файл: `components/MainBackground/main-bg.view.js`

```javascript
styles: {
    backgroundColor: '#5b7a9b',              // Цвет фона
    padding: '1rem',                         // Отступы
}
```

### 🏷️ **Изменить вид элемента категории**
Файл: `components/CategoryItem/category.view.js`

```javascript
styles: {
    backgroundColor: 'transparent',          // Фон по умолчанию
    backgroundColorHover: 'rgba(255, 255, 255, 0.15)',  // Фон при наведении
    colorIndicatorSize: '12px',              // Размер цветного кружка
    textColor: '#ffffff',                    // Цвет текста
    borderRadius: '6px',                     // Скругление
}
```

---

## ⚙️ Где менять ЛОГИКУ (поведение)

### 📅 **Изменить логику дня календаря**
Файл: `components/CalendarDay/day.logic.js`
- Количество видимых событий
- Обработка кликов
- Условия для CSS классов

### 🌐 **Изменить логику сетки**
Файл: `components/CalendarGrid/grid.logic.js`
- Названия дней недели
- Расчёт строк сетки

### 🏷️ **Изменить логику категории**
Файл: `components/CategoryItem/category.logic.js`
- Активность (фильтрация)
- Обработка кликов

---

## 🚀 Пример: Как изменить цвет дня при наведении

1. Открыть `components/CalendarDay/day.view.js`
2. Найти `backgroundColorHover`
3. Изменить на желаемый цвет

```javascript
backgroundColorHover: 'rgba(100, 150, 200, 0.3)',  // Новый цвет
```

---

## 🚀 Пример: Как изменить фон всего календаря

1. Открыть `components/CalendarGrid/grid.view.js`
2. Найти `backgroundColor`
3. Изменить на желаемый цвет

```javascript
backgroundColor: '#3d5a7a',  // Другой серо-синий
```
