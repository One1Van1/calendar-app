# Backend Structure - Визуальная карта

Полная структура Calendar Backend API.

```
backend/
│
├── 📖 backend-docs/
│   ├── INDEX.md                     # Быстрая навигация
│   ├── README.md                    # Главная документация
│   ├── STRUCTURE.md                 # Эта страница
│   ├── ACTION-INSTRUCTION.md        # Инструкция для AI-агента
│   └── API-REFERENCE.md             # Справочник по API
│
├── 🏗️ src/
│   │
│   ├── 🎯 features/                 # ИЗОЛИРОВАННЫЕ API ACTIONS
│   │   │
│   │   ├── events/                  # События календаря
│   │   │   ├── GET/
│   │   │   │   ├── get-all/        # GET /events
│   │   │   │   ├── get-by-id/      # GET /events/:id
│   │   │   │   └── get-by-range/   # GET /events/range?start=&end=
│   │   │   ├── POST/
│   │   │   │   └── create-event/   # POST /events
│   │   │   ├── PUT/
│   │   │   │   └── update-event/   # PUT /events/:id
│   │   │   └── DELETE/
│   │   │       └── delete-event/   # DELETE /events/:id
│   │   │
│   │   ├── categories/              # Категории
│   │   │   ├── GET/
│   │   │   │   ├── get-all/        # GET /categories
│   │   │   │   └── get-by-id/      # GET /categories/:id
│   │   │   ├── POST/
│   │   │   │   └── create-category/# POST /categories
│   │   │   ├── PUT/
│   │   │   │   └── update-category/# PUT /categories/:id
│   │   │   └── DELETE/
│   │   │       └── delete-category/# DELETE /categories/:id
│   │   │
│   │   └── reminders/               # Напоминания
│   │       ├── GET/
│   │       │   ├── get-all/        # GET /reminders
│   │       │   └── get-by-event/   # GET /reminders/event/:eventId
│   │       ├── POST/
│   │       │   └── create-reminder/# POST /reminders
│   │       └── DELETE/
│   │           └── delete-reminder/# DELETE /reminders/:id
│   │
│   ├── 📦 modules/                  # АГРЕГИРУЮЩИЕ МОДУЛИ
│   │   ├── events.module.ts
│   │   ├── categories.module.ts
│   │   └── reminders.module.ts
│   │
│   ├── 🗄️ entities/                 # СТАБИЛЬНЫЕ ENTITIES (ИММУТАБЕЛЬНЫЕ)
│   │   ├── event.entity.ts
│   │   ├── category.entity.ts
│   │   └── reminder.entity.ts
│   │
│   ├── 🔧 shared/                   # ОБЩИЕ КОМПОНЕНТЫ
│   │   ├── dto/
│   │   │   └── formatted-response.dto.ts
│   │   ├── enums/
│   │   │   ├── event.enum.ts
│   │   │   └── reminder.enum.ts
│   │   ├── repositories/
│   │   │   ├── event.repository.ts
│   │   │   ├── category.repository.ts
│   │   │   └── reminder.repository.ts
│   │   ├── interceptors/
│   │   └── decorators/
│   │
│   ├── 🔐 config/                   # КОНФИГУРАЦИЯ
│   │   ├── database.config.ts
│   │   └── app.config.ts
│   │
│   ├── app.module.ts                # Главный модуль
│   └── main.ts                      # Entry point
│
├── 🧪 test/                         # E2E ТЕСТЫ
│   ├── events/
│   ├── categories/
│   └── reminders/
│
├── 📝 package.json
├── 📝 tsconfig.json
├── 📝 nest-cli.json
└── 📝 yarn.lock
```

---

## 📊 Статистика

```
┌────────────────────────────────────────────────┐
│         BACKEND METRICS                        │
├────────────────────────────────────────────────┤
│  📄 Total Features:          ~12 actions       │
│  📁 Modules:                  3                │
│  🗄️ Entities:                 3                │
│  🔧 Shared Components:       ~10               │
│  📘 TypeScript:              100%              │
│  🏗️ Architecture:            Feature-based     │
│  ✅ Isolation:               100%              │
└────────────────────────────────────────────────┘
```

---

## 🏗️ Архитектура

```
REQUEST
   ↓
┌─────────────────────────────┐
│     CONTROLLER              │
│  (Routing + Validation)     │
└──────────┬──────────────────┘
           │
┌──────────┴──────────────────┐
│     SERVICE                 │
│  (Business Logic)           │
└──────────┬──────────────────┘
           │
┌──────────┴──────────────────┐
│   REPOSITORY / TYPEORM      │
│  (Data Access)              │
└──────────┬──────────────────┘
           │
┌──────────┴──────────────────┐
│     SQLITE DATABASE         │
│  (Local Storage)            │
└─────────────────────────────┘
```

---

## 🎯 Структура одной Feature Action

```
get-all/
├── get-all.controller.ts      # 🎮 Контроллер (routing)
├── get-all.service.ts          # 💼 Сервис (бизнес-логика)
├── get-all.query.dto.ts        # 📝 Query параметры
├── get-all.response.dto.ts     # 📤 Response DTO
├── get-all.spec.ts             # 🧪 E2E тесты
└── openapi.decorator.ts        # 📚 Swagger документация
```

**Принципы:**
- ✅ Один endpoint = одна папка
- ✅ Полная изоляция
- ✅ Никаких зависимостей между actions
- ✅ Переиспользование только стабильных entities/repositories

---

## 📋 API Endpoints Overview

### 📅 Events API
```
GET    /events              - Получить все события (с пагинацией)
GET    /events/:id          - Получить событие по ID
GET    /events/range        - Получить события за период
POST   /events              - Создать событие
PUT    /events/:id          - Обновить событие
DELETE /events/:id          - Удалить событие
```

### 🏷️ Categories API
```
GET    /categories          - Получить все категории
GET    /categories/:id      - Получить категорию по ID
POST   /categories          - Создать категорию
PUT    /categories/:id      - Обновить категорию
DELETE /categories/:id      - Удалить категорию
```

### ⏰ Reminders API
```
GET    /reminders                  - Получить все напоминания
GET    /reminders/event/:eventId   - Получить напоминания для события
POST   /reminders                  - Создать напоминание
DELETE /reminders/:id              - Удалить напоминание
```

---

## 🔐 Ключевые принципы архитектуры

### 1. Иммутабельность Entities ⛔
```typescript
// ❌ НИКОГДА не редактируйте
entities/event.entity.ts
entities/category.entity.ts
entities/reminder.entity.ts
```

### 2. Изоляция Actions 🔒
```typescript
// ❌ НИКОГДА не импортируйте
import { GetAllEventsService } from '../get-all/get-all.service';

// ✅ Только стабильные компоненты
import { Event } from '../../../entities/event.entity';
import { Repository } from 'typeorm';
```

### 3. Атомарность 🎯
```
Одна action = Один endpoint = Одна папка = Один тест
```

---

## 🚀 Быстрый старт для разработчика

### Добавить новый endpoint:

1. **Определите тип:** GET/POST/PUT/DELETE
2. **Создайте папку:**
   ```bash
   mkdir -p src/features/events/GET/get-upcoming
   ```
3. **Создайте файлы:**
   - `get-upcoming.controller.ts`
   - `get-upcoming.service.ts`
   - `get-upcoming.query.dto.ts`
   - `get-upcoming.response.dto.ts`
   - `get-upcoming.spec.ts`
   - `openapi.decorator.ts`

4. **Следуйте:** [ACTION-INSTRUCTION.md](./ACTION-INSTRUCTION.md)

5. **Тестируйте:**
   ```bash
   yarn test:e2e
   ```

---

## 📚 Документация

- 📖 [INDEX.md](./INDEX.md) - Быстрая навигация
- 📘 [ACTION-INSTRUCTION.md](./ACTION-INSTRUCTION.md) - Гайд для AI-агента
- 📗 [API-REFERENCE.md](./API-REFERENCE.md) - Справочник API
- 📕 [README.md](./README.md) - Общая документация

---

## 🎯 Workflow разработки

```
1. Создать feature action папку
         ↓
2. Написать Controller + Service + DTOs
         ↓
3. Добавить OpenAPI decorator
         ↓
4. Написать E2E тесты
         ↓
5. Добавить в Module
         ↓
6. Запустить тесты
         ↓
7. Code Review
         ↓
8. Merge
```

---

**Создано:** 30 ноября 2025 г.  
**Версия:** 1.0  
**Статус:** ✅ Ready for Development
