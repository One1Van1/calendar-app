# 🗓️ Calendar - Local Desktop Application

Локальное десктопное приложение-календарь на базе **Electron + NestJS + TypeScript + PostgreSQL**.

## 📋 Описание

Desktop приложение для управления событиями, категориями и напоминаниями с локальным хранением данных в PostgreSQL.

## 🚀 Технологии

- **Backend**: NestJS + TypeScript + TypeORM + PostgreSQL
- **Frontend**: Electron (Desktop App)
- **Менеджер пакетов**: Yarn
- **База данных**: PostgreSQL (локальная)

## 📦 Структура проекта

```
Calendar/
├── backend/          # NestJS REST API (16 endpoints)
│   ├── src/
│   │   ├── entities/      # Event, Category, Reminder
│   │   ├── features/      # Isolated endpoints
│   │   ├── config/        # Database & app config
│   │   └── shared/        # DTOs, enums
├── frontend/         # Electron приложение
│   ├── src/              # Main & preload process
│   └── renderer/         # UI (HTML/CSS/JS)
├── shared/           # Общие типы
└── global-plan/      # Документация
```

## 🛠️ Требования

- Node.js >= 18
- Yarn >= 1.22.x
- PostgreSQL >= 14
- macOS / Windows / Linux

## 📚 Установка

```bash
# Клонировать репозиторий
git clone https://github.com/One1Van1/calendar-app.git
cd Calendar

# Установить зависимости для backend
cd backend
yarn install

# Установить зависимости для frontend
cd ../frontend
yarn install
```

## 🗄️ Настройка базы данных

```bash
# Создать пользователя и базу данных
psql postgres
CREATE ROLE calendar_user WITH LOGIN PASSWORD 'calendar_password';
ALTER ROLE calendar_user CREATEDB;
CREATE DATABASE calendar_db OWNER calendar_user;
GRANT ALL PRIVILEGES ON DATABASE calendar_db TO calendar_user;
\q
```

## 🚀 Запуск

### Вариант 1: Desktop приложение (рекомендуется)
```bash
cd frontend
yarn dev
```
Backend запустится автоматически!

### Вариант 2: Раздельный запуск

**Backend:**
```bash
cd backend
yarn start:dev
```

**Frontend:**
```bash
cd frontend
yarn start
```

## 📖 API Документация

После запуска backend доступна Swagger документация:
**http://localhost:3000/api/docs**

### API Endpoints (16 total)

#### Events API
- `GET /events` - список событий с пагинацией
- `GET /events/:id` - событие по ID  
- `GET /events/range?startDate&endDate` - события за период
- `POST /events` - создать событие
- `PUT /events/:id` - обновить событие
- `DELETE /events/:id` - удалить событие

#### Categories API  
- `GET /categories` - список категорий
- `GET /categories/:id` - категория по ID
- `POST /categories` - создать категорию
- `PUT /categories/:id` - обновить категорию
- `DELETE /categories/:id` - удалить категорию

#### Reminders API
- `GET /reminders` - список напоминаний
- `GET /reminders/event/:eventId` - напоминания для события
- `GET /reminders/pending` - предстоящие напоминания
- `POST /reminders` - создать напоминание
- `DELETE /reminders/:id` - удалить напоминание

## 🎯 Основные функции

✅ Просмотр календаря по месяцам  
✅ Создание/редактирование/удаление событий  
✅ Категории с цветами (#HEX)  
✅ Напоминания о событиях  
✅ События на весь день  
✅ Фильтрация по категориям  
✅ Поиск по диапазону дат  
✅ Полная валидация данных  
✅ Swagger документация  

## 📦 Сборка приложения

```bash
cd frontend

# Сборка для текущей ОС
yarn dist

# Сборка для всех платформ
yarn dist --mac --win --linux
```

Готовые приложения: `frontend/release/`

## 🏗️ Архитектура

### Backend
- **Feature-based isolation** - каждый endpoint изолирован в отдельной папке
- **Immutability** - неизменяемые структуры данных
- **TypeORM** - автоматические миграции
- **Validation** - class-validator для всех входных данных

### Frontend  
- **Main Process** - запускает NestJS backend
- **Renderer Process** - UI календаря
- **IPC** - безопасное взаимодействие между процессами

## 📝 TODO

- [ ] Повторяющиеся события (daily/weekly/monthly)
- [ ] Системные уведомления  
- [ ] Drag & Drop событий
- [ ] Экспорт/импорт (.ics)
- [ ] Поиск по тексту
- [ ] Темная тема
- [ ] Горячие клавиши
- [ ] Иконка в трее

## 📚 Документация

- [План разработки](./backend/backend-docs/DEVELOPMENT-PLAN.md)
- [Инструкция для AI](./backend/backend-docs/ACTION-INSTRUCTION.md)
- [Структура проекта](./backend/backend-docs/STRUCTURE.md)

## 📝 Лицензия

MIT

## 👨‍💻 Автор

One1Van1

---

**Статус:** ✅ MVP готов  
**Версия:** 1.0.0  
**Последнее обновление:** 30 ноября 2025 г.
